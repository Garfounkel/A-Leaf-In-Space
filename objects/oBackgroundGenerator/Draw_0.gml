gpu_set_tex_filter(true); // only if it isn't on anyways
draw_sprite_stretched(sBG_Gradient2, 0, -100, -100, room_width + 200, room_height + 200);
gpu_set_tex_filter(false); // only if it isn't on anyways
